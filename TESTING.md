# Протокол тестирования

## Стенд

3 nginx в режиме reverse proxy + приложение traefik/whoami (выводит все заголовки запроса).
Все контейнеры в одной docker-сети 172.20.0.0/24 с фиксированными адресами.

Варианты маршрутизации:
- порт 8081 — пользователь → nginx1 → app
- порт 8082 — пользователь → nginx2 → nginx1 → app
- порт 8083 — пользователь → nginx3 → nginx2 → nginx1 → app

## Решение

Разграничиваем доверенные источники (другие nginx в цепочке) и недоверенные (пользователь напрямую).
В `nginx.conf` через модуль `geo` прописаны IP контейнеров nginx1–3 как доверенные.
Адрес docker-шлюза (172.20.0.1) туда не входит — запросы с хоста обрабатываются как пользовательские.

Через `map` формируем `X-Forwarded-For`:
- источник недоверенный → XFF перезаписывается в `$remote_addr`, входящий заголовок отбрасывается
- источник доверенный → `$remote_addr` дописывается к существующей цепочке

Конфиг у всех трёх nginx одинаковый, апстрим передаётся через переменную окружения
`UPSTREAM_TARGET` и подставляется через envsubst при старте контейнера.

## Запуск

```
docker compose up -d
docker compose ps
```

## Тесты

### 1. Запрос через один прокси

```
$ curl -s http://localhost:8081
Hostname: 8f5fc6e66dc6
IP: 172.20.0.10
RemoteAddr: 172.20.0.11:59950
GET / HTTP/1.1
Host: localhost
User-Agent: curl/8.7.1
X-Forwarded-For: 172.20.0.1
X-Real-Ip: 172.20.0.1
```

### 2. Цепочка из двух прокси

```
$ curl -s http://localhost:8082
...
X-Forwarded-For: 172.20.0.1, 172.20.0.12
RemoteAddr: 172.20.0.11:59962
```

### 3. Цепочка из трёх прокси

```
$ curl -s http://localhost:8083
...
X-Forwarded-For: 172.20.0.1, 172.20.0.13, 172.20.0.12
RemoteAddr: 172.20.0.11:59978
```

### 4. Защита от спуфинга, одиночный прокси

Пользователь пытается подставить свой X-Forwarded-For:

```
$ curl -s -H "X-Forwarded-For: 1.1.1.1" http://localhost:8081
...
X-Forwarded-For: 172.20.0.1
```

`1.1.1.1` не попал в приложение — nginx1 перезаписал XFF реальным `$remote_addr`.

### 5. Защита от спуфинга, цепочка из трёх прокси

```
$ curl -s -H "X-Forwarded-For: 1.1.1.1" http://localhost:8083
...
X-Forwarded-For: 172.20.0.1, 172.20.0.13, 172.20.0.12
```

Поддельный заголовок отброшен на nginx3, цепочка дальше строится штатно.

## Логи

nginx логирует `xff_in` (входящий заголовок) и `xff_out` (что передано дальше):

```
$ docker compose logs nginx1 | tail -5
172.20.0.1  xff_in="-"                          xff_out="172.20.0.1"
172.20.0.12 xff_in="172.20.0.1"                 xff_out="172.20.0.1, 172.20.0.12"
172.20.0.12 xff_in="172.20.0.1, 172.20.0.13"    xff_out="172.20.0.1, 172.20.0.13, 172.20.0.12"
172.20.0.1  xff_in="1.1.1.1"                    xff_out="172.20.0.1"
172.20.0.12 xff_in="172.20.0.1, 172.20.0.13"    xff_out="172.20.0.1, 172.20.0.13, 172.20.0.12"
```
